<!-- Generated by tools/Build-WrapperModule.ps1. Regenerate rather than edit by hand. -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- netstandard2.0 because this assembly has to load in both PowerShell editions. The
         compatibility contract is declared by the module every wrapper references,
         src/Authentication/Authentication/Microsoft.Graph.Authentication.psd1: PowerShellVersion
         '5.1' and CompatiblePSEditions 'Core','Desktop'. A module manifest binds one
         un-conditioned dll path, so a single framework has to satisfy both editions: net472
         covers Desktop only, net6.0/net8.0/net10.0 cover Core only, netstandard2.0 covers both.
         Microsoft.Kiota.Bundle 2.0.0 ships a netstandard2.0 asset, and the generated client
         compiles against it. -->
    <TargetFramework>netstandard2.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>Microsoft.Graph.Wrapper.Security.Client</AssemblyName>
    <NoWarn>$(NoWarn);CS1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Kiota.Bundle" Version="2.0.0" />
  </ItemGroup>

</Project>
